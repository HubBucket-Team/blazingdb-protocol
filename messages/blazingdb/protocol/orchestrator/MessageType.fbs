namespace blazingdb.protocol.orchestrator;

enum MessageType : byte { DDL = 0, DML, DDL_CREATE_TABLE, DDL_DROP_TABLE, AuthOpen, AuthClose, RegisterFileSystem, DeregisterFileSystem, LoadCsvSchema, LoadParquetSchema, DML_FS, SchemaList}
